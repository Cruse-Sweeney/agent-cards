name = "a2a-agents"
main = "src/index.js"
compatibility_date = "2026-06-01"

# agents.json is imported by src/index.js and bundled at deploy time, so the
# card content and the system prompts ship together and cannot drift.
[observability]
enabled = true
